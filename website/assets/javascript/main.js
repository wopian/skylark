(function($) {
    // Init Skrollr
    var s = skrollr.init({
        render: function(data) {
            // Debugging - log the current scroll position.
            // console.log(data.curTop);
        }
    });
})(jQuery);
